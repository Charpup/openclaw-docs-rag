# OpenClaw Docs RAG Configuration

# Database Configuration (Docker PostgreSQL + pgvector)
PGHOST=localhost
PGPORT=5432
PGUSER=memu
PGPASSWORD=memu_secure_password
PGDATABASE=memu_db

# OpenAI API Key for embeddings
# Set this in environment or replace here
OPENAI_API_KEY=${OPENAI_API_KEY}

# Optional: Custom table name
DOCS_TABLE=openclaw_docs_chunks
